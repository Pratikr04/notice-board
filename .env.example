# PostgreSQL connection string.
# Create a free database at https://neon.tech (no credit card needed) and paste
# the pooled connection string below. Supabase or any other hosted Postgres works too.
# It must start with postgresql:// for this project.
DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler.REGION.neon.tech/DB?sslmode=require"
